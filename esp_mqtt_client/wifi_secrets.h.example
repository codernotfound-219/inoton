#pragma once

// Copy this file to `wifi_secrets.h` (same folder) and fill in values.
// IMPORTANT: `wifi_secrets.h` is gitignored and must not be committed.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// MQTT broker IP/hostname (e.g. "192.168.1.10" or "broker.local")
#define MQTT_SERVER "YOUR_MQTT_BROKER"
