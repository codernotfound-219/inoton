# Frontend (Vite) backend connection settings
# Defaults are localhost:8000 if you omit these.

VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=8000
VITE_BACKEND_HTTP_PROTO=http
VITE_BACKEND_WS_PROTO=ws
